# PHP opcional

O site principal não depende de PHP. Use este endpoint apenas para testes locais de salvamento.

```bash
php -S localhost:8080 -t php
```

Depois envie um POST JSON para `/save-sheet.php`.
